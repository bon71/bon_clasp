{
  "name": "abilibrary",
  "version": "1.0.0",
  "main": "abi_library.gs",
  "scripts": {
    "format": "prettier --write '**/*.{js,ts,gs}'",
    "push": "git push && clasp push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.3.3"
  },
  "description": ""
}
